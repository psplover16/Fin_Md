## 一、 交易 (Transaction) 與 ACID 核心
*   **定義**：不可分割的工作單位，核心精神為「全部成功」或「全部失敗」。
*   **生命週期**：UPDATE(記憶體) ➔ COMMIT ➔ 寫 Log (交易結束) ➔ 寫硬碟。
*   **ACID 四大特性** (公式：**A + I + D = C**)：
    *   **A (原子性)**：全部完成或全不做 (失敗靠 `ROLLBACK` 復原)。
    *   **C (一致性)**：交易執行前後，資料庫都維持在符合所有完整性限制的合法狀態 (系統終極目標)。
    *   **I (隔離性)**：多個交易並行時互不干擾，排隊執行 (靠 Lock/MVCC 達成，最難維護)。
    *   **D (持久性)**：COMMIT 後永久保存 (靠 交易記錄檔 (Transaction Log) 達成)。

---

## 二、 讀取異常 與 隔離等級 (Isolation Levels)
### 三大讀取異常 (難以預防度：髒讀 < 不可重複讀 < 幻讀)
1.  **髒讀 (Dirty Read)**：讀到另一交易「未提交」的資料。
2.  **不可重複讀**：在同一交易內前後讀取同一筆資料，卻因其他交易在期間內完成「更新 (Update) 或刪除 (Delete)」，導致兩次讀取的數值或狀態不一致。
3.  **幻讀 (Phantom Read)**：在同一交易內前後執行相同的範圍查詢，卻因其他交易在期間內「新增 (Insert)」或「刪除 (Delete)」了符合條件的紀錄，導致第二次查詢得到的資料與之前不相同。幻讀僅靠序列化才能防禦。

### 四大隔離等級 (針對 SELECT 行為)
*   **Read Uncommitted (讀未提交)**：無防禦力。
*   **Read Committed (讀已提交)**：防髒讀。
*   **Repeatable Read (可重複讀)**：**MySQL 預設**。防不可重複讀。
*   **Serializable (序列化)**：強制循序排隊。效能極差，防所有異常 (含幻讀)。

---

## 三、 併發控制機制 (隔離性實作)
### MVCC (多版本併發控制)
*   **核心**：是一種資料庫系統用於處理並行控制的技術。核心概念為 **資料修改時不直接覆蓋，而是保留歷史版本**，使不同交易能讀取當前有效的快照，達成「讀寫互不阻塞」的底層機制。
*   **特性**
    *   **讀不阻塞寫，寫不阻塞讀**：查詢操作讀取歷史快照，更新操作則修改最新版本。雙方互不干擾。
    *   **空間換時間**：儲存歷史快照佔用空間，換取免等待的執行速度。

*   **MVCC 在 RR 與 RC 的決定性差異**
    *   **RC 等級**：每次 SELECT 產**新快照**。
    *   **RR 等級**：第一次 SELECT 產快照，後續**沿用舊快照** (達成可重複讀)。

### 鎖(悲觀鎖+2PL)
*   **悲觀鎖**：透過對資料加鎖限制多個交易同時存取同一資源。其特性為「用排隊換正確性」 (如 `FOR UPDATE`)。

### 樂觀鎖
*   **樂觀鎖**：不是鎖，事後檢查。假設少衝突，UPDATE 時才核對版本。


---

## 四、 鎖定機制與 Gap Lock (悲觀鎖細節)
*   **S-Lock (共享鎖)**：唯讀不寫 / **X-Lock (排他鎖)**：獨佔讀寫。
*   **自動加鎖**：SELECT 預設無鎖；UPDATE/DELETE 自動加鎖。
*   **死結陷阱**：同交易內先 `FOR SHARE` 後 `UPDATE` 極易死結，應直接 `FOR UPDATE`。

<!-- ### Gap Lock (間隙鎖) 與 Next-Key Lock 觸發條件 (RR 等級下)
*   **定義**：並非 SQL 標準規範，而是 MySQL (InnoDB) 引擎特有的底層實作技術。主要目的是防幻讀。
*   **Gap Lock**：禁止其他交易 於 兩個索引記錄之間的「間隙」插入新資料，藉此防止幻讀
    *   **觸發機制**：範圍查詢、查無資料、**非唯一索引精準命中**。
    *   **無 Gap Lock**：RC 等級，或 RR 等級下「唯一索引精準命中」。
*   **Next-Key Lock**：同時鎖定「索引記錄本身」以及「該記錄前方的間隙」，也就是 左開右閉區間 (a, b]
    *   **防止**：在RR層級下，能防止 不可重複讀 與 幻讀
    *   **觸發機制**：InnoDB 預設行為(RR層級)、範圍與非唯一查詢 -->

---

## 五、 二階段鎖定協定 (2PL)
*   **目標**：用來保證多個交易並行十，衝突可序列化。
*   **標準 2PL (絕不交錯)**：
    *   **擴張階段**：只能**申請**鎖，絕不可釋放。
    *   **收縮階段**：只能**釋放**鎖，絕不可申請 (釋放第一把鎖即進入此階段)。
*   **標準 2PL 缺點**：
    1.  **連鎖復原 (Cascading Rollback)**：*(最致命缺點)* 允許在 `COMMIT` 前提早解鎖。若交易最終 `ROLLBACK`，會導致讀到半成品資料的其他交易也被迫取消，引發效能災難。
    2.  **無法避免死結 (Deadlock)**：交易在擴張期可能互相等待對方手上的鎖，導致永久卡死。

### 嚴格 2PL (Strict 2PL) - 實務採用
*   **機制**：所有 X-Lock 必須撐到 **COMMIT / ROLLBACK 的那一刻** 才能釋放。
*   **優缺點**：解決連鎖復原問題，但也付出了效能代價：
    1.  **併發度大幅下降 (Reduced Concurrency)**：鎖被強制扣留到最後一刻，其他交易等待時間變長，導致系統整體**吞吐量**下降。
    2.  **死結機率更高**：因鎖抓得比標準 2PL 更久，死結的機率隨之增加。

---

## 六、 實戰程式碼考點

### 1. TCL 交易指令
```sql
START TRANSACTION;
SAVEPOINT p1;          -- 設存檔點
ROLLBACK TO p1;        -- 部分還原
COMMIT;                -- 永久寫入
```

### 2. 樂觀鎖更新防護 (防線在 WHERE)
```sql
UPDATE accounts SET balance = balance - 500, version = version + 1 
WHERE id = 1 AND version = 1; -- 必須與取出的版本相符
```

### 3. 變數作用域陷阱
*   `@a`：**連線變數** (Session)，隨處可用。
*   `DECLARE a`：**區域變數** (Local)，僅限 Stored Procedure/Function 的 BEGIN...END 內。